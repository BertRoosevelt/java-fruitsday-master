# 功能完成度对照表（FEATURES_CHECKLIST）

> 天天果园 FruitDay 水果销售电商系统  
> 本文档根据需求规格说明书，对照当前代码实际情况，梳理功能完成状态。

---

## 目录

1. [已实现功能详细列表](#1-已实现功能详细列表)
2. [未实现功能清单](#2-未实现功能清单)
3. [优先级建议](#3-优先级建议)
4. [实现说明](#4-实现说明)

---

## 1. 已实现功能详细列表

### 1.1 前台消费者端 ✅ 已实现功能

| # | 功能点 | 完成状态 | 实现文件 | 备注 |
|---|-------|---------|---------|------|
| 1.1 | 用户注册 | ✅ 完成 | `reg.jsp` + `UserServlet.doAdd()` + `UserDaoImpl.add()` | 注册时自动创建购物记录表 |
| 1.2 | 用户登录（邮箱/手机） | ✅ 完成 | `login.jsp` + `UserServlet.doLogin()` + `UserDaoImpl.findByStr()` | 支持邮箱和手机号登录 |
| 1.3 | 管理员身份识别 | ✅ 完成 | `UserServlet.doLogin()` + `UserService.root()` | 登录时自动检测管理员权限 |
| 1.4 | 商品列表展示（热卖） | ✅ 完成 | `index.jsp` + `FruitService.hot()` + `FruitDaoImpl.findHot()` | 主页展示热卖商品 |
| 1.5 | 商品详情页 | ✅ 完成 | `fruit_info.jsp` + `FruitServlet?key=info` | 含名称、价格、简介、封面等 |
| 1.6 | 商品搜索/筛选 | ✅ 完成 | `sel.jsp` + `SELServlet` | 支持关键词搜索 |
| 1.7 | 加入购物车 | ✅ 完成 | `ShopServlet?key=add&str=cart` + `ShopDaoImpl` | 在商品详情页操作 |
| 1.8 | 查看购物车 | ✅ 完成 | `ShopServlet?key=show&boob=cart` + `showcart.jsp` | 展示购物车商品列表 |
| 1.9 | 删除购物车商品 | ✅ 完成 | `ShopServlet?key=del&str=cart` | 从购物车移除商品 |
| 1.10 | 加入收藏夹 | ✅ 完成 | `ShopServlet?key=add&str=star` | 在商品详情页操作 |
| 1.11 | 查看收藏夹 | ✅ 完成 | `ShopServlet?key=show&boob=star` + `showstar.jsp` | 展示收藏商品列表 |
| 1.12 | 取消收藏 | ✅ 完成 | `ShopServlet?key=del&str=star` | 从收藏夹移除商品 |

### 1.2 后台管理员端 ✅ 已实现功能

| # | 功能点 | 完成状态 | 实现文件 | 备注 |
|---|-------|---------|---------|------|
| 2.1 | 后台登录入口 | ✅ 完成 | `UserServlet.doLogin()` → `BSindex.jsp` | 管理员登录自动跳转 |
| 2.2 | 查看用户列表 | ✅ 完成 | `BSServlet?key=alluser` → `BSindex1.jsp` | 显示所有注册用户 |
| 2.3 | 添加用户 | ✅ 完成 | `BSindex2.jsp` → `BSServlet?key=adduser` | 管理员手动创建账号 |
| 2.4 | 删除用户 | ✅ 完成 | `BSServlet?key=deluser&id={id}` | 直接删除，无确认弹窗 |
| 2.5 | 查看用户详情 | ✅ 完成 | `BSServlet?key=finduser&id={id}` → `BSindex3.jsp` | 含编辑表单 |
| 2.6 | 修改用户信息 | ⚠️ 部分完成 | `BSServlet?key=upuser` | 代码调用了 `add()` 而非 `upUser()`，逻辑有问题 |
| 2.7 | 查看所有商品列表 | ✅ 完成 | `BSServlet?key=allfruit` → `BSindex4.jsp` | 含名称、规格、单价 |
| 2.8 | 查看热卖商品 | ✅ 完成 | `BSServlet?key=hotfruit` → `BSindex4.jsp` | 查询 hotfruits 关联表 |
| 2.9 | 添加商品 | ✅ 完成 | `BSindex5.jsp` → `BSServlet?key=addfruit` | 需手动填写商品ID |
| 2.10 | 删除商品 | ✅ 完成 | `BSServlet?key=delfruit&fid={fid}` | 直接删除，无确认弹窗 |
| 2.11 | 查看商品详情 | ✅ 完成 | `BSServlet?key=findfruit&fid={fid}` → `BSindex6.jsp` | 含编辑表单 |
| 2.12 | 修改商品信息 | ✅ 完成 | `BSServlet?key=upfruit&fid={fid}` | 支持修改所有字段 |

---

## 2. 未实现功能清单

### 2.1 前台消费者端 ❌ 未实现功能

| # | 功能点 | 未实现原因 | 需要新增的文件/表 |
|---|-------|----------|----------------|
| 3.1 | 购物车修改数量 | `ShopServlet.doNum()` 仅统计数量，未实现修改 | 需修改 `shop{id}` 表结构，增加 `quantity` 字段 |
| 3.2 | 购物车一键结算 | 无订单系统，无法结算 | 需新增订单表和订单Servlet |
| 3.3 | 下单生成订单 | 未建立订单数据模型 | 需新增 `orders`、`order_items` 表 |
| 3.4 | 订单状态查看 | 无订单功能 | 需订单管理系统 |
| 3.5 | 在线支付 | 无支付集成 | 需接入支付宝/微信支付SDK |
| 3.6 | 确认收货 | 无订单状态流转 | 依赖订单系统 |
| 3.7 | 历史订单查询 | 无订单记录 | 依赖订单系统 |
| 3.8 | 商品评分 | 无评价数据表 | 需新增 `reviews` 表 |
| 3.9 | 发布商品评论 | 无评论功能 | 需新增 `comments` 表 |
| 3.10 | 评论回复 | 无评论功能 | 依赖评论系统 |
| 3.11 | 评论点赞/取消点赞 | 无点赞功能 | 需新增 `comment_likes` 表 |
| 3.12 | 删除本人评论 | 无评论功能 | 依赖评论系统 |
| 3.13 | 个人中心-个人信息 | 无个人中心页面 | 需新增 `profile.jsp` 或扩展功能 |
| 3.14 | 个人中心-支付记录 | 无支付功能 | 依赖支付系统 |
| 3.15 | 个人中心-收货地址管理 | 无地址功能 | 需新增 `address` 表 |
| 3.16 | 修改密码 | 无密码修改功能 | 需新增修改密码页面和Servlet |
| 3.17 | 按分类浏览商品 | 无商品分类字段 | `fruits` 表需增加 `category` 字段 |
| 3.18 | 商品平均评分展示 | 无评价系统 | 依赖评价系统 |
| 3.19 | 商品评论数量展示 | 无评论系统 | 依赖评论系统 |

### 2.2 后台管理员端 ❌ 未实现功能

| # | 功能点 | 未实现原因 | 需要新增的文件/表 |
|---|-------|----------|----------------|
| 4.1 | 用户权限角色管理 | 管理员判断逻辑简单（ID白名单），无角色体系 | 需新增 `roles` 表，完善权限管理 |
| 4.2 | 商品分类管理 | `fruits` 表无分类字段 | 需新增 `categories` 表，`fruits` 加 `category_id` |
| 4.3 | 商品图片管理 | 仅有 `inum`（图片数量）字段，无图片上传 | 需实现文件上传功能 |
| 4.4 | 商品促销价管理 | 无促销字段 | `fruits` 表需增加 `promo_price`、`promo_end` 字段 |
| 4.5 | 查看全量订单 | 无订单系统 | 依赖订单系统 |
| 4.6 | 审核异常订单 | 无订单系统 | 依赖订单系统 |
| 4.7 | 退款/退货管理 | 无退款流程 | 依赖订单系统和支付系统 |
| 4.8 | 手动触发发货 | 无物流系统 | 需新增发货状态管理 |
| 4.9 | 优惠券管理 | 无优惠系统 | 需新增 `coupons` 表 |
| 4.10 | 限时折扣配置 | 无活动系统 | 需新增 `promotions` 表 |
| 4.11 | 满减活动管理 | 无活动系统 | 依赖活动系统 |
| 4.12 | 查看全量评论 | 无评论系统 | 依赖评论系统 |
| 4.13 | 删除违规评论 | 无评论系统 | 依赖评论系统 |
| 4.14 | 销售排行报表 | 无统计功能 | 需新增统计查询和图表展示 |
| 4.15 | 订单趋势报表 | 无订单系统 | 依赖订单系统 |
| 4.16 | 用户活跃度报表 | 无统计功能 | 需新增日志/埋点系统 |

### 2.3 已实现但存在缺陷的功能

| # | 功能点 | 缺陷描述 | 影响程度 |
|---|-------|---------|---------|
| 5.1 | 修改用户信息 | `BSServlet.doUpuser()` 调用了 `UserService.add()` 而非 `UserService.upUser()`，实际执行了插入而非更新 | 🔴 严重（数据错误） |
| 5.2 | 用户密码安全 | 密码以明文存储在数据库，存在安全风险 | 🔴 严重（安全问题） |
| 5.3 | 删除操作无确认 | 删除用户/商品无二次确认弹窗，易误操作 | 🟡 中等（用户体验） |
| 5.4 | 商品ID手动输入 | 添加商品时 `fid` 需手动填写，容易重复或遗漏 | 🟡 中等（易用性） |
| 5.5 | 错误处理缺失 | 添加用户失败时无提示；登录失败时可能空指针 | 🟡 中等（稳定性） |
| 5.6 | 删除用户不清理关联数据 | 删除用户后，其购物记录表（`shop{id}`）未同步删除 | 🟡 中等（数据一致性） |
| 5.7 | URL路径硬编码 | JSP中大量 `/x-test/BSServlet?...` 写死了应用名 | 🟢 轻微（部署灵活性） |
| 5.8 | 购物车数量修改未完成 | `ShopServlet.doNum()` 仅返回数量，不能修改 | 🟡 中等（功能不完整） |
| 5.9 | Session空指针风险 | 未登录直接访问购物车等页面时可能 NPE | 🔴 严重（稳定性） |

---

## 3. 优先级建议

### P0 - 必须立即修复（缺陷修复）

| 任务 | 影响 | 工作量 | 说明 |
|------|------|--------|------|
| 修复修改用户逻辑 | 修改用户实际变为添加，数据错误 | 小（改一行代码） | 将 `BSServlet.doUpuser()` 中的 `UserService.add()` 改为 `UserService.upUser()` |
| 修复登录空指针 | 用户不存在时程序崩溃 | 小 | `UserServlet.doLogin()` 中在使用 `u` 之前先判断 `u != null` |
| 修复Session空指针 | 未登录访问敏感页面崩溃 | 小 | 各功能页面增加 `session.getAttribute("user") != null` 判断 |

### P1 - 优先开发（核心功能补全）

| 任务 | 影响 | 工作量 | 说明 |
|------|------|--------|------|
| 密码加密 | 密码安全漏洞 | 中 | 使用 MD5 或 BCrypt 对密码哈希存储 |
| 订单系统 | 电商核心流程缺失 | 大 | 新增订单表、订单Servlet、订单页面 |
| 购物车数量修改 | 购物车功能不完整 | 小 | 修改 ShopServlet 和 shop 表结构 |
| 删除确认弹窗 | 防误操作 | 小 | JS confirm() 或 Modal 弹窗确认 |

### P2 - 次优先开发（功能增强）

| 任务 | 影响 | 工作量 | 说明 |
|------|------|--------|------|
| 商品分类 | 浏览体验提升 | 中 | 增加分类表，商品表加分类字段 |
| 商品图片上传 | 当前无法展示真实图片 | 中 | 实现文件上传Servlet |
| 个人中心 | 用户体验 | 中 | 添加个人信息查看/编辑页面 |
| 修改密码 | 账号安全 | 小 | 添加修改密码表单和处理逻辑 |

### P3 - 后续迭代（高级功能）

| 任务 | 影响 | 工作量 | 说明 |
|------|------|--------|------|
| 评价/评论系统 | 用户互动 | 大 | 新增评论表、点赞表、评论Servlet |
| 支付系统集成 | 实际交易能力 | 大 | 接入第三方支付API |
| 统计分析报表 | 运营辅助 | 大 | 增加统计查询和ECharts可视化 |
| 优惠券/活动系统 | 营销功能 | 大 | 新增活动管理模块 |
| 权限角色管理 | 系统安全 | 中 | 重构权限系统，支持多角色 |

---

## 4. 实现说明

### 4.1 技术架构实现情况

| 架构要素 | 计划 | 实现 | 说明 |
|---------|------|------|------|
| B/S 结构 | ✅ | ✅ | 浏览器/服务器架构 |
| MVC 模式 | ✅ | ✅ | JSP（V）+ Servlet（C）+ Service/DAO（M） |
| 三层架构 | ✅ | ✅ | 表现层/业务逻辑层/数据访问层 |
| 前端 JSP | ✅ | ✅ | 所有页面均为 JSP |
| Java Servlet | ✅ | ✅ | 5个Servlet |
| JDBC 数据库访问 | ✅ | ✅ | 使用 DBUtils + PreparedStatement |
| MySQL 数据库 | ✅ | ✅ | fruitday 数据库 |
| 会话管理 | ✅ | ✅ | HttpSession 存储用户状态 |

### 4.2 数据库建设情况

| 数据表 | 用途 | 状态 | 问题 |
|-------|------|------|------|
| `user` | 用户信息 | ✅ 完整 | 密码明文存储 |
| `fruits` | 商品信息 | ✅ 完整 | 无分类字段、fid 非自增 |
| `hotfruits` | 热卖商品标记 | ✅ 完整 | 正常 |
| `shop1` / `shop{id}` | 购物/收藏记录 | ⚠️ 部分 | 无数量字段，每用户单独建表（不规范） |
| `orders` | 订单表 | ❌ 缺失 | 未创建 |
| `order_items` | 订单商品表 | ❌ 缺失 | 未创建 |
| `categories` | 商品分类表 | ❌ 缺失 | 未创建 |
| `comments` | 评论表 | ❌ 缺失 | 未创建 |
| `coupons` | 优惠券表 | ❌ 缺失 | 未创建 |
| `address` | 收货地址表 | ❌ 缺失 | 未创建 |

### 4.3 功能完成度统计

**前台消费者端**

```
总需求功能数：19项
已完成：      12项  ████████████░░░░░░░  63%
未完成：       7项  ░░░░░░░░░░░░░░░░░░░  37%
```

**后台管理员端**

```
总需求功能数：16项
已完成：      11项（含1项有缺陷）  ██████████░░░░░░░░░  69%
未完成：       5项（加技术未实现） ░░░░░░░░░░░░░░░░░░░  31%
```

**整体完成度**

```
总功能需求：35项
已实现：    23项  ██████████████░░░░░  66%
未实现：    12项  ░░░░░░░░░░░░░░░░░░░  34%
```

### 4.4 快速修复指南（P0 级缺陷）

#### 修复1：修改用户逻辑错误

**文件：** `src/main/java/com/fruitDayDB/servlet/BSServlet.java`

```java
// 当前错误代码（第180行附近）：
User boo = UserService.add(user);

// 修改为：
boolean boo = UserService.upUser(user);
if (boo) {
    doAlluser(req, resp);
}
```

#### 修复2：登录空指针保护

**文件：** `src/main/java/com/fruitDayDB/servlet/UserServlet.java`

```java
// 当前代码（第66行附近）在 u 可能为 null 时直接调用 u.getId()
// 修改 doLogin() 方法：
User u = UserService.login(str, pwd, boo);

if (u == null) {
    req.getRequestDispatcher("/login.jsp").forward(req, resp);
    return;
}

// 然后再检查管理员...
List<Integer> uids = UserService.root();
```

#### 修复3：Session 未登录保护

在所有需要登录才能访问的页面（如 `showcart.jsp`、`showstar.jsp`）添加：

```jsp
<%
  if(session.getAttribute("user") == null) {
    response.sendRedirect("login.jsp");
    return;
  }
%>
```

---

## 总结

本项目已实现了电商系统的基础骨架（用户注册登录、商品浏览、购物车收藏、后台用户和商品管理），完成度约 **66%**。

核心缺失的功能模块是：
1. **订单系统**（最核心的电商功能）
2. **评价/评论系统**
3. **支付集成**
4. **数据统计分析**

建议按 P0 → P1 → P2 → P3 的顺序逐步完善，优先修复现有缺陷，再逐步增加新功能。

---

*文档最后更新：2026-04*
